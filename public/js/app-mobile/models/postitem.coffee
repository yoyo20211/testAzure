define(['backbone'],
        (Backbone) ->
            # Using ECMAScript 5 strict mode during development. By default r.js will ignore that.
            "use strict"

            PostItem = Backbone.Model.extend({})

            return PostItem
)