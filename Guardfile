# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{views/.+\.(html|jade)})
  watch(%r{public/stylesheets/.+\.(css|less)})
  watch(%r{public/pages/.+\.(html)})
  watch(%r{public/js/.+\.(js)})
end
