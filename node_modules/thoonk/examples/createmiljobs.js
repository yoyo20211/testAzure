var Thoonk = require("../thoonk").Thoonk;
var thoonk = new Thoonk();
var testjob = thoonk.job('testjob5');
var idx = 0;


var publishit = function() {
        var test = {
                    process: "function(data){ console.log(data);}"

            };
        //console.log('x')
        var obj = JSON.stringify(test);
        console.log(obj);
        testjob.publish(obj, function(err, item, id) {
            console.log('publish already'); 
        },false,'yah1');
};
testjob.once("ready", function() {
    publishit();
});
